AWS_ENDPOINT_URL=http://localhost:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_S3_ADDRESSING_STYLE=path
# Optional LocalStack Pro token:
# LOCALSTACK_AUTH_TOKEN=ls-xxxxxxxx
LOCALSTACK_AUTH_TOKEN=

# LocalStack runs should stay fast and avoid huge BLS downloads (e.g. `ln.data.1.AllData`).
# This overrides `.env.shared` for local runs only.
#
# Note: we include `ln` because the curated site charts depend on unemployment
# and participation series; the fetcher materializes a small `ln.data.0.Current`
# extract via the BLS API (not the huge file mirror).
BLS_SERIES=pr,cu,jt,ci,ln

# Optional explicit bucket overrides (normally derived from FOMC_BUCKET_PREFIX in .env.shared):
# BLS_BUCKET=fomc-bls-raw
# DATAUSA_BUCKET=fomc-datausa-raw
# BLS_PROCESSED_BUCKET=fomc-bls-processed
# DATAUSA_PROCESSED_BUCKET=fomc-datausa-processed
